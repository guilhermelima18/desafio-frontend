import { Oval } from "react-loader-spinner";

export const Loading = () => {
  return (
    <Oval
      width="70"
      height="70"
      color="var(--primary-color)"
      ariaLabel="loading"
    />
  );
};
